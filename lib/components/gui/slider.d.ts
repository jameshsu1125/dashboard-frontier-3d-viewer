import { TSlider } from '../../settings/type';

type TProps = {
    data: TSlider;
};
declare const Slider: import('react').MemoExoticComponent<({ data }: TProps) => import("react/jsx-runtime").JSX.Element>;
export default Slider;
